<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="FullScreenTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <!-- Swipes browse cards instead of closing the screen -->
        <item name="android:windowSwipeToDismiss">false</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
